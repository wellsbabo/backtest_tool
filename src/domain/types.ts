export type DateString = `${number}-${number}-${number}`;

export type CurrencyCode = "KRW" | "USD" | string;

export type MarketCode = "KRX" | "US" | string;

export type TradeAction = "buy" | "sell";

export type PositionSide = "flat" | "long";
